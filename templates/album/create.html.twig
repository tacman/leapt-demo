{% extends 'album/layout.html.twig' %}

{% block body %}
    <h1>Create an album</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.photos) }}

        <div>
            <button type="submit" class="btn btn-primary">Create the album</button>
        </div>
    {{ form_end(form) }}
{% endblock body %}
